SET(LIB_NAME OsgPdf)
SET(PLUGIN_HEADERS
    OsgPdf.h
)

ADD_LIBRARY(${LIB_NAME}
    "SHARED"
    ${PLUGIN_HEADERS}
    OsgPdf.cpp
)

FIND_PATH(PDFREADER_INCLUDE_DIR PdfReader.h)

INCLUDE_DIRECTORIES(${OSG_INCLUDE_DIR} ${PDFREADER_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(${LIB_NAME} ${OSG_LIBRARIES})

INSTALL(TARGETS ${LIB_NAME} DESTINATION lib/plugins)

